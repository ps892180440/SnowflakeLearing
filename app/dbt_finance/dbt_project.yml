name: 'dbt_finance'
version: '1.0.4'
profile: 'dbt_finance'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  dbt_finance:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
