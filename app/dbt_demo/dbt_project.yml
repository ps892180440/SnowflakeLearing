name: 'dbt_demo'
version: '1.0.0'
profile: 'dbt_demo'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  dbt_demo:
    staging:
      +materialized: view
    marts:
      +materialized: table
